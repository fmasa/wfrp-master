<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="menu_labels_style">
        <item name="android:paddingLeft">4dp</item>
        <item name="android:paddingRight">4dp</item>
        <item name="android:paddingTop">2dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:background">@drawable/fab_label</item>
        <item name="android:textColor">@color/colorFabLabelText</item>
        <item name="android:elevation">6dp</item>
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">13sp</item>
    </style>
    <style name="TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/colorPrimaryDark</item>
        <item name="android:background">@color/colorCardBackground</item>
    </style>
    <style name="BottomNavigationView.Active" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

</resources>
